out/*
release/*
node_modules
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*/dist/*
dist/*
.DS_Store
native/**/target
native/target-rust-analyzer
native/**/.build
out.json
tsconfig.node.tsbuildinfo
server/deploy-dev.sh

# Sentry Config File
.env.sentry-build-plugin
.electron-builder-cache
.npm-cache
.cache
.history
.mcp.json

# Vendored third-party reference apps (not part of the build)
opensrc/

# Agent handoff notes and scratch files
tmp/

# Stray build-output backups (dist/* above does not cover sibling names,
# and electron-builder swallows anything left at the repo root into app.asar)
dist.*/
